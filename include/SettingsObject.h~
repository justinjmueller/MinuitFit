//
//-Function Definitions file name.
//-Minimizer step size.
//-Minimizer precision.
//-Minimizer max calls.
//-Minimizer UP.

//C++ includes.
#include <iostream> //Basic input and output.
#include <string> //Basic string.
#include <map> //STL map.

class SettingsObject
{
 public:
  SettingsObject(std::string SettingsFile);
  std::string Query(std::string Key);
  friend ostream& operator<<(ostream& os, const SettingsObject& Obj)
 private:
  std::map<std::string, std::string> SettingsMap;
};
